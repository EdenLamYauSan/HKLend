{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd /Users/yslam/hklend && pnpm tsc --noEmit 2>&1 | tail -20"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys,json,os; p=os.environ.get('CLAUDE_TOOL_INPUT','{}'); d=json.loads(p); path=d.get('file_path',''); blocked=['.env.local','prisma/migrations']; found=[x for x in blocked if x in path]; print('BLOCKED: do not edit '+found[0]) if found else None; exit(2 if found else 0)\""
          }
        ]
      }
    ]
  }
}
