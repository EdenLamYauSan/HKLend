{
  "name": "hklend-scraper",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.0.0"
  },
  "description": "Isolated scraper workspace — Playwright and Chromium binaries live here, never in the app dependency tree.",
  "scripts": {
    "scrape": "tsx src/scrape-hkma.ts",
    "scrape:news": "tsx src/scrape-news.ts",
    "install-browsers": "playwright install chromium"
  },
  "dependencies": {
    "pinyin-pro": "3.28.1"
  },
  "devDependencies": {
    "playwright": "1.52.0",
    "tsx": "4.19.4",
    "typescript": "5.9.3"
  },
  "notes": "ARCH-3: This package is physically isolated from src/. Any shared type must be duplicated here — never import from ../../src."
}
